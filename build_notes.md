# Build Notes

1. Created the public `cyber-ghost` repository.
2. Added normal learning and project files.
3. Added `secret.txt` & `documentation.txt` containing the real flag.
4. Committed the file.
5. Deleted `secret.txt` & `documentation.txt` in a later commit.
6. Linked the repository through LinkedIn.
7. Verified that `docs/solve.py` recovers the flag.
